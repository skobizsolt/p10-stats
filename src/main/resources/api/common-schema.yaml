openapi: 3.1.0
info:
  title: Common Schema API
  version: 1.0.0
servers:
  - url: http://localhost:8080
paths: {}
components:
  schemas:
    Error:
      type: object
      properties:
        code:
          type: string
        message:
          type: string
      required:
        - code
        - message